# Signify_sign2text — Python dependencies
# Pinned to the verified training environment:
#   Python 3.10.20 | CUDA 12.1 | Windows 11
#
# Install:  pip install -r requirements.txt
# Note: PyTorch is pinned to the cu121 wheel.
#       If your CUDA version differs, replace the +cu121 suffix
#       or install PyTorch separately from https://pytorch.org/get-started

# ── Core ML ──────────────────────────────────────────────────────────────
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121
--extra-index-url https://download.pytorch.org/whl/cu121

# ── Data / numerics ──────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.3

# ── Landmark extraction (MediaPipe) ──────────────────────────────────────
# Only needed if re-extracting landmarks from video.
# Skip if using the pre-extracted .npy files from Hugging Face.
mediapipe==0.10.14
opencv-python==4.10.0.84

# ── Notebook ─────────────────────────────────────────────────────────────
jupyter==1.1.1
ipykernel==6.29.5

# ── Utilities ────────────────────────────────────────────────────────────
tqdm==4.66.5
matplotlib==3.9.2
scikit-learn==1.5.2
